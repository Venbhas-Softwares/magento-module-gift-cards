/**
 * Superseded by Venbhas\GiftCard\Ui\DataProvider\Product\Modifier\GiftCardOptions
 * which handles "Use Config Settings" checkboxes through the standard Magento
 * UI component modifier pattern. This module is kept as a no-op to avoid
 * breaking the x-magento-init reference in the layout XML.
 */
define([], function () {
    'use strict';

    return function () {};
});
