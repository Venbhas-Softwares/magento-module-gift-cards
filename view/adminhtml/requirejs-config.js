/**
 * Register grid cell template for store scope column.
 */
var config = {
    paths: {
        // Resolved after Magento_Ui template loader prefix (Vendor_Module/template/grid/...).
        'Venbhas_GiftCard/template/grid/cells/store-scope':
            'Venbhas_GiftCard/templates/grid/cells/store-scope'
    }
};
